name: Daily Briefing

on:
  schedule:
    # Run off-the-hour to avoid GH Actions cron contention (top-of-hour
    # scheduled jobs are frequently skipped on free tier due to high load).
    # 09:00 Vilnius (morning preview): 06:07 UTC summer, 07:07 UTC winter
    - cron: '7 6 * * 1-5'
    - cron: '7 7 * * 1-5'
    # 22:00 Vilnius (evening recap): 19:07 UTC summer, 20:07 UTC winter
    - cron: '7 19 * * 1-5'
    - cron: '7 20 * * 1-5'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  send-briefing:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium

      - name: Run daily briefing
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          REDDIT_CLIENT_ID: ${{ secrets.REDDIT_CLIENT_ID }}
          REDDIT_CLIENT_SECRET: ${{ secrets.REDDIT_CLIENT_SECRET }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
        run: |
          cd src && python main.py

      - name: Commit web version
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git add docs/
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Web briefing $(date -u +%Y-%m-%d) [skip ci]"
          for i in 1 2 3; do
            git pull --rebase --strategy-option=theirs --no-edit && git push && exit 0
            sleep 5
          done
          echo "Push failed after 3 retries" && exit 1

